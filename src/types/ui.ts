import {ReactNode} from 'react'

export interface MyComponentChildrenArray {
    children: ReactNode;
  }

  export interface LoaderType {
    height: string;
    width: string;
    radius: string;
    color: string;
  }